# RAG知识库问答系统 - 依赖说明

# 核心依赖
streamlit>=1.28.0        # Web界面框架
requests>=2.28.0         # HTTP请求（大模型API调用）
numpy>=1.21.0            # 向量计算

# 向量数据库
faiss-cpu>=1.7.0         # FAISS向量索引（推荐，高效）

# 文档解析
pdfplumber>=0.10.0       # PDF解析（推荐，保留布局）
# PyPDF2>=3.0.0          # PDF解析（备选）
python-docx>=0.8.11      # Word文档解析

# ── 一键安装 ─────────────────────────────────────────────────────────────────
# pip install -r requirements.txt

# ── 最小安装（无PDF/DOCX）────────────────────────────────────────────────────
# pip install streamlit requests numpy

# ── 运行 ─────────────────────────────────────────────────────────────────────
# streamlit run app.py
